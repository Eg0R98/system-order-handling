syntax = "proto3";

package inventory;


// gRPC-сервис в inventory-service с методом для проверки наличия товаров
service InventoryService {
    // Проверка наличия товаров
    rpc checkAvailability (ProductsRequest) returns (ProductsResponse);
}

// DTO для передачи информации о товаре из order-service в inventory-service
message ProductOrderServiceDTO {
    string productId = 1; // Идентификатор товара
    string name = 2;      // Название товара
    int32 quantity = 3;   // Запрошенное количество
}

// DTO для успешной проверки наличия товара со стороны inventory-service
message SuccessfulProductInventoryServiceDTO {
    string productId = 1;               // Идентификатор товара
    string name = 2;                    // Название товара
    string discountedPrice = 3;        // Цена со скидкой
    string totalValueWithDiscount = 4; // Общая стоимость с учетом количества и скидки
    int32 quantity = 5;                // Доступное количество
    string sale = 6;                   // Скидка
}

// DTO для товаров, которые не прошли проверку (недоступны или недостаточно количества)
message UnsuccessfulProductInventoryServiceDTO {
    string name = 1; // Название товара
}

// Запрос от сервиса заказов в инвентарь на проверку наличия товаров
message ProductsRequest {
    repeated ProductOrderServiceDTO products = 1; // Список товаров для проверки
}

// Ответ от инвентаря с разделением на успешные и неуспешные товары
message ProductsResponse {
    repeated SuccessfulProductInventoryServiceDTO successfulProducts = 1;   // Найденные товары
    repeated UnsuccessfulProductInventoryServiceDTO unsuccessfulProducts = 2; // Товары, которых нет
}


